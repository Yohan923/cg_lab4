#version 330 core

in vec2 TexCoord;

uniform sampler2D _Texture;

out vec4 FragColor;

void main()
{
	vec3 lightColor = vec3(1.0, 1.0, 1.0);
    float ambientStrength = 1.0;
    vec3 ambient = ambientStrength * lightColor;

    FragColor =  texture(_Texture, TexCoord) * vec4(ambient, 1.0);
}